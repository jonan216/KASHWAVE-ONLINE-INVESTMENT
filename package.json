{
  "name": "kashwave-root",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm run build && cp public/_redirects dist/_redirects",
    "postinstall": "cd frontend && npm install"
  },
  "dependencies": {},
  "devDependencies": {}
}
